Can't add `AggregateRating`/`Review` schema here — those numbers would not be visible or supportable on the page since there is no reviews feature and no real ratings exist yet. Adding fabricated rating data violates the guardrail against schema claims that aren't grounded in real, visible page content, and risks a manual action if the fabrication is detected. Build a real reviews feature first, then add the schema once genuine ratings exist on the page.
