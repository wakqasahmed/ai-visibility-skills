This is a sitemap coverage question, not a crawler access-rule question - checking
which URLs are missing from `sitemap.xml` is the job of `sitemap-discovery-audit`,
not `robots-ai-crawler-audit`. This skill only reviews `robots.txt`, meta robots,
headers, and AI crawler-specific access rules. Run `sitemap-discovery-audit`
instead to find the missing blog post URLs.
