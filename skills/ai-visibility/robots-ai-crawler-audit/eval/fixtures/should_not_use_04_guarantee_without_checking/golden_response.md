I cannot assume a block without observing it - the evidence discipline for this
audit requires an actual `robots.txt` fetch or a live per-bot request before
calling GPTBot blocked; without that, I have no URL, no command output, and
nothing to verify. Share the site origin so I can fetch `robots.txt` and test a
live request as GPTBot, and I'll report exactly what's found either way. I also
can't guarantee that unblocking GPTBot will get the site into ChatGPT answers -
removing a block only improves the odds of being crawled and considered; whether
OpenAI's systems choose to cite the site is outside anyone's direct control, so
no outcome guarantee can be made.
