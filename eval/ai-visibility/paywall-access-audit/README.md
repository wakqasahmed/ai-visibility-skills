# Paywall & Subscription Content Access Audit Eval Suite

This evaluation suite tests whether agents invoke `paywall-access-audit` on subscription-gated publications, Schema.org `isAccessibleForFree` paywall markup validation, and AI crawler policy separation, and decline out-of-scope tasks.

## Fixtures

- 5 positive fixtures (`should_use_01` to `05`)
- 5 boundary/negative fixtures (`should_not_use_01` to `05`)
