{
  "id": "should_use_04_edge_waf_block_gptbot",
  "category": "should_use",
  "description": "robots.txt is permissive, but a fetch with GPTBot's documented full request user-agent returns 403 while PerplexityBot's documented full user-agent and a default user-agent return 200; the result must remain a derived anti-spoofing/WAF warning until operator log/IP evidence verifies a real crawler block.",
  "expects_full_report": true
}
