Auditing `Product`/`Offer` structured data (JSON-LD completeness, price, availability, review
schema) is a different skill's job — `schema-markup-audit` — not this one. This skill only
covers ecommerce catalog spot-checks: thin category pages, faceted-navigation duplicate URLs,
orphan pages, and discontinued-product 404/redirect handling. Ask `schema-markup-audit` for the
structured-data review instead.
