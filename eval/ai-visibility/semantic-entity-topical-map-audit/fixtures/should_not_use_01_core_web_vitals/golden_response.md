This request is out of scope for `semantic-entity-topical-map-audit`. LCP, CLS, and INP are page-delivery performance metrics measured from CrUX field data or a lab run such as PageSpeed Insights; this skill inspects entity identity markup (`@id`, `sameAs`) and pillar-cluster internal linking, and has no way to measure paint or input-latency timings. Core Web Vitals are scored under the orchestrator's Technical Accessibility pillar — please refer to `ai-visibility-audit` for that measurement.
