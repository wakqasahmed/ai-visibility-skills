Run the commerce-protocol-discovery probes on `https://docs.example.dev`. It's a SaaS
documentation site — no `Product`/`Offer` schema anywhere, no cart, no checkout, no "add to
cart" text, just API reference pages and a pricing page for the subscription plans.
