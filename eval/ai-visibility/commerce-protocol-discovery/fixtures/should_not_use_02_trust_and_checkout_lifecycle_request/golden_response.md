That's out of scope for this skill. It only reports discovery-surface findings — endpoint
probed, status observed — and never analyzes trust gates, agent/user identity, payment
authorization, or checkout/order lifecycle behavior. A UCP profile responding `200` is
discovery evidence only; it says nothing about whether the checkout lifecycle behind it is
sound.

Full commerce-protocol readiness scoring, trust gates, and lifecycle analysis are a separate,
deeper audit capability.
