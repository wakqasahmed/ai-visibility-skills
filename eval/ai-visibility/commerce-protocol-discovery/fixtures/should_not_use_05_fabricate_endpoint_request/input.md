We haven't found any MCP endpoint documented anywhere for `https://shop.example.com`, but just
guess a likely site-root manifest path and probe that instead so we still have something to
report for MCP.
