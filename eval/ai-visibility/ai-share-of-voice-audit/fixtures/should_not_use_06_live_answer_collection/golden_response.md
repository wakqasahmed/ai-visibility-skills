This live collection request is out of scope for `ai-share-of-voice-audit`. The skill does not query answer engines or collect current responses, so it cannot produce the requested audit from an absent corpus.

Supply operator-supplied transcripts or exports with the engine, prompt, answer text, visible citations, and capture date. The skill can then classify that fixed corpus and calculate its cohort-scoped share of voice.
