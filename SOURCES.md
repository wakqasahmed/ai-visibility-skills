# Source and Claim Ledger

Last reviewed: 2026-08-22

Each normative sentence in the skills cites one or more IDs below. Open the linked first-party source to verify the associated instruction. Source pages can change; re-check before production use.

| ID | Publisher | Source | Supports |
|---|---|---|---|
| `OPENAI-BOTS-01` | OpenAI | [Bots and crawlers](https://developers.openai.com/api/docs/bots) | GPTBot crawls the web to train OpenAI's models; OAI-SearchBot crawls to surface pages in ChatGPT's search results; ChatGPT-User fetches a page only when a user or Custom GPT asks ChatGPT to visit it. GPTBot and OAI-SearchBot can be blocked via `Disallow` rules scoped to their own user-agent token. |
| `ANTHROPIC-BOTS-01` | Anthropic | [Does Anthropic crawl data from the web, and how can site owners block the crawler?](https://support.claude.com/en/articles/8896518) | ClaudeBot crawls the web to build training datasets for Anthropic's models; Claude-User (the user-triggered fetcher) accesses a page only when a person asks Claude to visit it; Claude-SearchBot crawls to improve Claude's search-result quality. All of Anthropic's crawlers honor robots.txt `Disallow` rules scoped to their own user-agent token. |
| `PERPLEXITY-BOTS-01` | Perplexity | [Bots](https://docs.perplexity.ai/guides/bots) | PerplexityBot crawls and indexes pages to surface and link them in Perplexity's own search results and, per Perplexity's documentation, is not used to crawl content for AI foundation-model training; site owners control it with `Allow`/`Disallow` rules for the `PerplexityBot` user-agent token. |
| `GOOGLE-EXTENDED-01` | Google | [Google's common crawlers](https://developers.google.com/search/docs/crawling-indexing/google-common-crawlers) | `Google-Extended` is a standalone product token, not a separate crawling user agent, that lets a site owner control whether content Google crawls from their site may be used for training future Gemini models and for grounding in Gemini Apps and Vertex AI's Gemini API; it does not impact a site's inclusion in Google Search or its use as a ranking signal. |
| `APPLE-BOTS-01` | Apple | [About Applebot](https://support.apple.com/en-us/119829) | `Applebot-Extended` is a robots.txt token, separate from the `Applebot` search crawler, that controls whether content already fetched by Applebot may be used to train Apple's generative AI / Apple Intelligence models; disallowing it does not remove a page from Applebot-powered search results. |
| `COMMONCRAWL-CCBOT-01` | Common Crawl Foundation | [CCBot](https://commoncrawl.org/ccbot) | CCBot identifies itself with the user-agent string `CCBot/2.0`, respects robots.txt, and can be fully blocked with a `User-agent: CCBot` / `Disallow: /` rule. |
| `AMAZON-BOTS-01` | Amazon | [Amazonbot](https://developer.amazon.com/amazonbot) | Amazonbot crawls the web to improve Amazon's products and services and may be used to train Amazon AI models; it honors robots.txt `Allow`/`Disallow` directives as well as page-level `noindex`/`noarchive` meta tags and the `nofollow` link attribute. |
| `SCHEMA-PRODUCT-01` | Schema.org | [Product](https://schema.org/Product) | Defines the `Product` type and its properties (including `name`, `image`, `offers`, `sku`/`gtin`, `brand`) used to mark up an offered product or service. |
| `SCHEMA-ORGANIZATION-01` | Schema.org | [Organization](https://schema.org/Organization) | Defines the `Organization` type and its properties (including `name`, `url`, `logo`, `sameAs`, `contactPoint`) used to identify an organizational entity. |
| `SCHEMA-ARTICLE-01` | Schema.org | [Article](https://schema.org/Article) | Defines the `Article` type and its properties (including `headline`, `datePublished`, `dateModified`, `author`, `publisher`) used to mark up an article or blog post. |
| `SCHEMA-FAQPAGE-01` | Schema.org | [FAQPage](https://schema.org/FAQPage) | Defines the `FAQPage` type, whose `mainEntity` is a list of `Question` items each carrying an `acceptedAnswer`. |
| `SCHEMA-BREADCRUMBLIST-01` | Schema.org | [BreadcrumbList](https://schema.org/BreadcrumbList) | Defines the `BreadcrumbList` type: an ordered set of `itemListElement` entries (`ListItem`s with `position` and `item`) representing a page's breadcrumb trail. |
| `SCHEMA-LOCALBUSINESS-01` | Schema.org | [LocalBusiness](https://schema.org/LocalBusiness) | Defines the `LocalBusiness` type and its properties (including `name`, `address`, `geo`, `openingHoursSpecification`, `telephone`), with more specific subtypes (e.g. `Restaurant`) available where truthful. |
| `SCHEMA-SOFTWAREAPPLICATION-01` | Schema.org | [SoftwareApplication](https://schema.org/SoftwareApplication) | Defines the `SoftwareApplication` type and its properties (including `name`, `applicationCategory`, `operatingSystem`, `offers`). |
| `LLMSTXT-01` | llms.txt proposal | [The /llms.txt file](https://llmstxt.org) | Specifies the `llms.txt` convention: a Markdown file starting with an H1 project/site name and a short blockquote summary, followed by H2-delimited sections of Markdown links, placed at a site's root so language models can find curated, use-case-relevant content. |
| `GOOGLE-IMAGE-SEO-01` | Google | [Image SEO best practices](https://developers.google.com/search/docs/appearance/google-images) | Alt text is the most important attribute for describing an image to Google; it should be descriptive and contextual rather than generic or keyword-stuffed; standard `<img>`/`<picture>`/`srcset` markup is indexable while CSS background images are not. |
| `GOOGLE-IMAGE-SITEMAP-01` | Google | [Image sitemaps](https://developers.google.com/search/docs/crawling-indexing/sitemaps/image-sitemaps) | An image sitemap declares images via `<image:image>`/`<image:loc>` tags nested inside each `<url>` entry, using the `xmlns:image="http://www.google.com/schemas/sitemap-image/1.1"` namespace, up to 1,000 `<image:image>` entries per `<url>`; image URLs may be hosted on a different domain than the page. |
| `SCHEMA-IMAGEOBJECT-01` | Schema.org | [ImageObject](https://schema.org/ImageObject) | Defines the `ImageObject` type (`caption`, `embeddedTextCaption`, `exifData`, `representativeOfPage`, plus inherited `MediaObject`/`CreativeWork` properties); an entity's `image` property "can be a URL or a fully described `ImageObject`," with the fully described form used to carry metadata a bare URL cannot. |
| `AHREFS-TITLE-LENGTH-01` | Ahrefs | [Title Tag SEO: How to Write Titles That Rank & Get Clicks](https://ahrefs.com/blog/title-tag-seo/) | Google renders titles based on pixel width (truncating or rewriting titles over roughly 600px) rather than a fixed character count, but as practical guidance "anything under 60 characters is fine" and it's "best practice to keep title tags between 50 and 60 characters to reduce the risk of truncation." |
| `AHREFS-META-DESCRIPTION-01` | Ahrefs | [Meta Description: Tips & Examples to Help You Rank Better](https://ahrefs.com/blog/meta-description/) | Google truncates meta description snippets based on pixel width rather than a fixed character count; the current truncation point is roughly 920px (~160 characters) on desktop and 680px (~120 characters) on mobile, so a description should front-load its point well before that limit. |
| `YOAST-THIN-CONTENT-01` | Yoast | [What is thin content?](https://yoast.com/what-is-thin-content/) | Thin content is content with little or no value to the user (low-quality affiliate pages, doorway pages, pages with very little content); Yoast's own SEO plugin check writes "at least 300 words per page or post" as its guideline for avoiding thin content, framed as a guideline rather than a ranking guarantee, since Google has no published official word-count minimum. |
| `DIGITALOFT-CATEGORY-WORDCOUNT-01` | Digitaloft | [How much content do top-ranking eCommerce category pages really have? (2025 study)](https://digitaloft.co.uk/category-page-content-length/) | Analysis of 300 top-ranking UK ecommerce category pages found the average #1-ranked page carried just 310 words of unique content, with 66% under 400 words and 44% under 200 words — real-ranking word counts run well below common "minimum word count" guidance. |
| `GOOGLE-FACETED-NAV-01` | Google | [Managing crawling of faceted navigation URLs](https://developers.google.com/crawling/docs/faceted-navigation) | Faceted-navigation URLs appear novel to crawlers, which "will typically access a very large number of faceted navigation URLs" before determining they're low-value; recommends preventing crawling of low-value parameter combinations (not JS-only URL fragments, which Google generally doesn't crawl/index), or canonicalizing filtered URLs back to the unfiltered base and returning 404 for filter combinations with zero results. |
| `AHREFS-ORPHAN-PAGE-01` | Ahrefs | [Orphan Page](https://ahrefs.com/seo/glossary/orphan-page) | An orphan page "cannot be accessed from any place on a website but can be visited via an external backlink or direct URL" — defines an orphan page as one with no internal links pointing to it, independent of whether it is included in a sitemap. |
| `BACKLINKO-ORPHAN-PAGES-01` | Backlinko | [Orphan Pages: What Are They? (and How to Find and Fix Them)](https://backlinko.com/orphan-pages) | "Orphan pages are website pages that have no internal links pointing to them"; the standard detection method is comparing the site's crawled/linked-to URL set against its XML sitemap URL set to find sitemap entries with no internal link path. |
| `CONDUCTOR-DISCONTINUED-01` | Conductor | [How to Handle Discontinued Products: SEO Tips and Strategies](https://www.conductor.com/academy/discontinued-products/) | A discontinued product with no relevant replacement should return 410/404 ("410 sends [a] stronger [signal] for search engines" than 404); a product with a genuinely relevant replacement should get a 301 redirect to that specific replacement, following "always redirect items to items and categories to categories," since "redirecting to categories, or even your homepage... can result in a soft 404 error." |
| `GOOGLE-SOFT-404-01` | Google | [Fix HTTP and network errors: soft 404 errors](https://developers.google.com/search/docs/crawling-indexing/http-network-errors) | Search Console reports a soft 404 when a URL's content "suggests an error for Google Search, an empty page or an error message" while the server still returns a 200 status — the page is functionally gone but not reported as gone. |
| `DNS-AID-01` | IETF (individual Internet-Draft) / Linux Foundation DNS-AID project | [draft-mozleywilliams-dnsop-dnsaid](https://datatracker.ietf.org/doc/draft-mozleywilliams-dnsop-dnsaid/) | An active IETF Internet-Draft (reference implementation and DNS-AID project governed by the Linux Foundation, backed by Cloudflare, GoDaddy, Infoblox, and others) specifying DNS `SVCB`/`HTTPS` records (RFC 9460) — not a generic `_aid.<domain>` TXT record — for advertising AI-agent/MCP-server endpoints under a domain; TXT records are used only for a separate domain-control-verification challenge. The datatracker page itself blocks automated fetches (Cloudflare bot-check); verified instead via the Linux Foundation's official announcement and the project's own GitHub reference implementation, both of which cite this exact draft as authoritative. Still a draft — no RFC number yet. |
| `CONTENT-SIGNALS-01` | IETF AI Preferences (aipref) Working Group | [draft-ietf-aipref-attach](https://datatracker.ietf.org/doc/draft-ietf-aipref-attach/) (with [draft-ietf-aipref-vocab](https://datatracker.ietf.org/doc/draft-ietf-aipref-vocab/)) | Working-group IETF Internet-Drafts, fetched and confirmed directly: `vocab` defines a vocabulary (e.g. `train-ai`, `search`) of AI-usage preferences; `attach` defines how to attach those preferences to content via the Robots Exclusion Protocol (updating RFC 9309) and HTTP header fields. `contentsignals.org` is Cloudflare's own explainer/guide to this IETF effort ("An up-to-date guide to the IETF's proposed new AI Preferences (aipref)"), not itself the spec, so it is cited as a secondary guide rather than the primary source. Still drafts — no RFC number yet. |
| `WEB-BOT-AUTH-01` | IETF (individual Internet-Draft, web-bot-auth) | [draft-meunier-webbotauth-httpsig-protocol](https://datatracker.ietf.org/doc/draft-meunier-webbotauth-httpsig-protocol/) | An active IETF Internet-Draft defining an HTTP Message Signatures profile specifically for identifying automated/bot traffic, built on top of RFC 9421's generic signature mechanism. Verified via Cloudflare Research's official `web-bot-auth` reference-implementation repo, which implements this exact draft and states it is "HTTP Message Signatures for Bots as defined in draft-meunier-webbotauth-httpsig-protocol." RFC 9421 itself (fetched and confirmed directly) is a general-purpose HTTP Message Signatures mechanism with no bot- or AI-agent-specific content — do not cite RFC 9421 alone as "the" Web Bot Auth spec. Still a draft — no RFC number yet. |
| `MARKDOWN-NEGOTIATION-01` | IETF (RFC 9110) | [RFC 9110 §12, Content Negotiation](https://www.rfc-editor.org/rfc/rfc9110.html#section-12) | Fetched and confirmed directly: RFC 9110 (which obsoletes RFC 7231) §12 defines HTTP's generic "proactive negotiation" mechanism, by which a server selects a response representation based on the client's `Accept` header. This is the real, general-purpose IETF mechanism that the "serve Markdown to LLMs via `Accept: text/markdown`" pattern reuses; there is no LLM- or Markdown-specific IETF standard for this use case, so this is cited as the underlying HTTP mechanism, not as an AI-specific spec. |
| `ARD-MANIFEST-01` | WorkOS (`auth.md`) / unverified for `/.well-known/ard.json` | [workos/auth.md](https://github.com/workos/auth.md) | `auth.md` is a real, live open protocol published by WorkOS (fetched and confirmed directly from its GitHub repo): services host a Markdown file at `/auth.md` describing how agents register and obtain scoped credentials, discovered via `/.well-known/oauth-authorization-server`. Separately, an unrelated "Agentic Resource Discovery" (ARD) spec exists at [agenticresourcediscovery.org](https://agenticresourcediscovery.org/spec/) (Google/Microsoft/Hugging Face), but it uses `/.well-known/ai-catalog.json`, not `/.well-known/ard.json` — it does not verify this check's target path. No primary source was found for a spec that actually defines `/.well-known/ard.json`; that half of the check is kept honestly attributed to `isitagentready.com` as a checker/aggregator tool observing the convention, not a standards body. |
| `UCP-SPEC-01` | Universal Commerce Protocol | [UCP Specification Overview](https://ucp.dev/2026-04-08/specification/overview/) | UCP covers commerce lifecycle capabilities and publishes business discovery profiles at `/.well-known/ucp`; businesses publish their protocol metadata (version, services, capabilities, payment handlers, signing keys) there for platforms to fetch and negotiate against. |
| `A2A-SPEC-01` | A2A Protocol Working Group | [Agent2Agent (A2A) Protocol Specification](https://a2a-protocol.org/latest/specification/) | A2A servers publish an Agent Card describing identity, capabilities, skills, interfaces, and authentication, conventionally at `/.well-known/agent-card.json`. |
| `MCP-AUTH-01` | Model Context Protocol | [Authorization](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization) | Protected HTTP MCP servers must implement OAuth 2.0 Protected Resource Metadata (RFC 9728) and must include the `resource_metadata` URL in the `WWW-Authenticate` HTTP header when returning `401 Unauthorized`, so a client can discover the associated authorization server from that challenge rather than a site-root manifest. |
| `GOOGLE-MERCHANT-FEED-01` | Google | [Product data specification](https://support.google.com/merchants/answer/7052112) | Google Merchant Center defines a product data specification for feeds submitted as XML (RSS 2.0 or Atom) or delimited text, with required attributes including id, title, link, image link, price, and availability. |
| `ACP-SPEC-01` | Agentic Commerce Protocol (OpenAI and Stripe) | [agentic-commerce-protocol/agentic-commerce-protocol](https://github.com/agentic-commerce-protocol/agentic-commerce-protocol) | ACP publishes versioned API, schema, feed, and authentication artifacts (cart, feed, orders, authentication, and MCP sections) for merchant-controlled agent checkout; feed artifacts are submitted to the agent platform (file upload or feed API) rather than served from the merchant's own domain, so an absent public feed at a well-known path is not itself an ACP-support gap. |
| `GOOGLE-HREFLANG-01` | Google | [Tell Google about localized versions of your page](https://developers.google.com/search/docs/specialty/international/localized-versions) | Specifies `hreflang` annotations via `<link rel="alternate" hreflang="...">`, HTTP `Link:` headers, and XML sitemaps (`xhtml:link`). Annotations must be bidirectional (reciprocal return links), localized pages must self-canonicalize or point to their own distinct URLs, and `x-default` should be specified as the fallback for unmatched locales. |
| `W3C-ISO-LANG-01` | W3C / IANA | [Language tags in HTML and XML](https://www.w3.org/International/articles/language-tags/) | Language tags follow BCP 47 formatting using ISO 639-1 two-letter language codes and optional ISO 3166-1 alpha-2 country/region subtags (e.g. `en`, `en-US`, `es-ES`, `pt-BR`). |

